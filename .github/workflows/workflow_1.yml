# steps:

name: Python-actions-sample
on: [push]

env:
  JOB_VARIABLE: blue

jobs:

  sample-job-1:
    name: Test Checkout
    runs-on: ubuntu-latest

    steps:
      - name: Use the variable
        run: echo $JOB_VARIABLE

