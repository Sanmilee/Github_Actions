# steps:

name: Python-actions-sample
on: [push]
jobs:

  sample-job-1:
    name: Test Checkout
    runs-on: ubuntu-latest

    env:
      JOB_VARIABLE: red

    steps:
      - name: Use the variable
        run: echo $JOB_VARIABLE

        