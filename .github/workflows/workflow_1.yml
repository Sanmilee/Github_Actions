# steps:

name: Python-actions-sample
on: [push]


jobs:

  sample-job-1:
    name: Test Checkout
    runs-on: windows-latest

    steps:
      - run: dir
        if: runner.os == 'ubuntu'  # ${{ runner.os == 'windows' }}
